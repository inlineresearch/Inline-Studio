[project]
name = "inline-studio-frontend"
version = "1.2.1"
description = "Prebuilt Inline Studio web UI (SPA), served by Inline Core. Mirrors comfyui-frontend-package."
requires-python = ">=3.9"
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# The payload is the built SPA under inline_studio_frontend/static/ (index.html + hashed assets),
# which is a build output and gitignored. `artifacts` tells hatch to include those otherwise-ignored
# files (in both the sdist and the wheel) exactly once - using force-include here instead would add
# them a second time on top of `packages`, which errors with a duplicate index.html.
[tool.hatch.build]
artifacts = ["inline_studio_frontend/static/**"]

[tool.hatch.build.targets.wheel]
packages = ["inline_studio_frontend"]
